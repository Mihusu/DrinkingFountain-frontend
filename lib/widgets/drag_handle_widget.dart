/*

// File: widgets/drag_handle_widget.dart

import 'package:flutter/material.dart';
import 'package:toerst/themes/app_colors.dart';

// The pill you drag the fountain list with
class DragHandleWidget extends StatelessWidget {
  const DragHandleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 55.0,
      height: 6.0,
      margin: const EdgeInsets.symmetric(vertical: 8.0),
      decoration: BoxDecoration(
        color: draggablePillColor,
        borderRadius: BorderRadius.circular(4.0),
      ),
    );
  }
}

*/
